%li
  = comment.body
  - if comment.user_id == current_user.id
    = button_to "delete", movie_comment_path(movie_id: comment.movie.id, id: comment.id), method: :delete
  \-
  %small
    Submitted #{time_ago_in_words(comment.created_at)} ago
